# Build trees
build/
build-*/
cmake-build-*/
out/
install/

# CMake
CMakeCache.txt
CMakeFiles/
CMakeUserPresets.json
cmake_install.cmake
compile_commands.json
.cmake/

# Third-party binaries fetched at configure time rather than vendored
third_party/onnxruntime/

# Exported voices. Derived artefacts, around a gigabyte each; regenerate with
# `cd tools && python -m rvcara_export <voice>`. The directory itself is kept, with its
# README, so the search path exists in a fresh clone.
res/models/*
!res/models/README.md
*.onnx
*.ort
*.hnsw
retrieval.bin

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Editors and tooling
.vscode/
.idea/
.cache/
.DS_Store
*.swp

# Local render output from the verification pass
output/
